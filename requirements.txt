fastapi
uvicorn
sqlalchemy
psycopg2-binary  # For PostgreSQL, or sqlite3 if testing locally
python-jwt       # For JWT
emails           # For SMTP (optional, replace with sendgrid if preferred)
flask
flask-cors
pytest
requests
python-dotenv